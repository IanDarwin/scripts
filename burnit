#!/bin/sh

# burn a CD

if [ ! -w / ]; then
	echo be root
	exit 1
fi

set -e

SPEED=12

ISOSIZE=-isosize
ISOSIZE=""

DUMMY=-dummy
DUMMY=""

cdrecord -v $DUMMY speed=${SPEED} dev=/dev/rcd0c:0,0,0 $ISOSIZE ${1-./*.fs}

mount -t cd9660 /dev/cd0c /mnt

ls . /mnt

umount /dev/cd0c

eject  /dev/cd0c
