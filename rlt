#!/bin/sh

# reload a PF table

[ -w / ] || {
	echo "Be root for $0"
	exit 1
}

for TBL
do

	echo "Table ${TBL} before:"
	pfctl -T show -t ${TBL} || exit

	sudo pfctl -T replace -t ${TBL}

	echo "Table ${TBL} after:"
	pfctl -T show -t ${TBL} || exit

done
