#!/bin/sh

# Enter or list the todo database

case $# in

0)	ssh dos.new psql -d todo <<!
	select id,name from task where status != 'c' order by creationdate;
!
	;;

*)	ssh dos.new psql -d todo <<!
	insert into task(name) values('$*');
!
	;;

esac

