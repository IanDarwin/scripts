#!/usr/local/jdk-11/bin/java --source 11

// Example of using Java with a shebang

public class HelloWorld {
  public static void main(String[] args) {
    System.out.println("hello " + (args.length > 0 ? args[0] : "world"));
  }
}

